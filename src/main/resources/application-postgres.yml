spring:
  datasource:
    url: ${POSTGRES_URL:jdbc:postgresql://localhost:5432/oneshop}
    username: ${POSTGRES_USERNAME:postgres}
    password: ${POSTGRES_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
